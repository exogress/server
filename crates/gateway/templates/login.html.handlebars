<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>This site requires authorization</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.1/css/bulma.min.css">
    <style>
        .block:not(:last-child) {
            margin-bottom: 2.5rem;
        }

        .block {
            text-align: center;
        }
    </style>
</head>

<body>
<section class="section is-large">
    <div class="columns is-mobile is-centered">
        <div class="column is-half">
            <div class="block">
                <h1 class="title">This site requires authorization</h1>
            </div>

            <div class="block">
                <aside class="menu">
                    <ul class="menu-list">
                        {{#each providers}}
                            <li>
                                <a href="{{ this.link }}">
                                    <img style="height: 32px; margin-right: 10px;"
                                         src="https://cdn.jsdelivr.net/npm/simple-icons@v3/icons/{{ this.name }}.svg">
                                    <span class="subtitle">{{ this.name }}</span>
                                </a>
                            </li>
                        {{/each}}
                    </ul>
                </aside>
            </div>
        </div>
    </div>
</section>
<footer class="footer">
    <div class="content has-text-centered">
        <p>
            Powered by <a href="https://exogress.com">Exogress</a>
        </p>
    </div>
</footer>
</body>
</html>
